/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.unsplash.com",
      "fe-pub.s3.ap-southeast-1.amazonaws.com",
      "i.pravatar.cc",
    ],
  },
};

export default nextConfig;
